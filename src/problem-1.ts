{
    //problem-1
    function sumArray(numbers: number[]): number {
      return numbers.reduce((acc, currentValue) => acc + currentValue, 0);
    }
    // const myNumber: number[] = [4, 7, 9, 3, 1, 20];
    // console.log(sumArray(myNumber));
  
    //
  }
  